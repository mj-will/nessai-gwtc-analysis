# GWTC-1 analysis with nessai

This analysis makes use of `nessai`, `bilby`, `bilby_pipe` and their various dependencies.

## Settings

Reference time is set to the detector with highest SNR based on the PyCBC triggers.